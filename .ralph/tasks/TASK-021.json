{
  "id": "TASK-021",
  "phase": "P3",
  "title": "Snapshot parser → compacted Observation (visible text + interactive refs)",
  "status": "pending",
  "pattern": "A",
  "acceptance": [
    "packages/runner/src/observe/compact-snapshot.ts: compactSnapshot(rawSnapshot) → Observation (per docs/04 §Observations) with visibleText, interactiveElements (role, label, selector, boundingBox)",
    "Drops nested generics; keeps interactive refs + visible text + price/error tokens",
    "Stores raw snapshot verbatim for replay (caller persists to .personabench/runs/<runId>/observations/<step>.json)",
    "Tests: a fixture agent-browser snapshot JSON parses into the compacted shape and round-trips with no dropped refs"
  ],
  "files": [
    "packages/runner/src/observe/compact-snapshot.ts",
    "packages/runner/src/__tests__/compact-snapshot.test.ts"
  ],
  "depends": ["TASK-020"]
}
